<%= notice %>

<!--サイドバー-->



<!--投稿詳細-->
<h2>Book detail</h2>
<table>
  <tbody>
    <tr>
      <td>
        <%= link_to user_path(@user) do %>
          <%= attachment_image_tag @book.user, :profile_image, fallback: 'no_image.jpg' %>
          <%= @user.name %>
        <%= end %>
      </td>
      <td><%= @book.title %></td>
      <td><%= @book.body %></td>
    </tr>
  </tbody>
</table>